# Flexible lower bounds so the app installs on any Python 3.10-3.13 host
# (exact pins like torch==2.5.1 have no wheels on newer Python versions and
# break Streamlit Cloud rebuilds).
#
# For CPU-only deployments, torch can be installed from the CPU wheel index
# to avoid pulling CUDA libraries:
#   pip install torch --index-url https://download.pytorch.org/whl/cpu
torch>=2.5,<3
transformers>=4.46,<5
pillow>=10.4
tqdm>=4.66
faiss-cpu>=1.9,<2
numpy>=1.26,<3
imagehash>=4.3.1,<5
streamlit>=1.52,<2
pandas>=2.2,<3
plotly>=5.24,<7
networkx>=3.4,<4
gdown>=5.2,<6
